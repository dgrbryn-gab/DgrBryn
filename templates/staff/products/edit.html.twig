{% extends 'staff/staff_base.html.twig' %}

{% block title %}Edit Product - Staff Dashboard - CORK{% endblock %}

{% block page_title %}Edit Product{% endblock %}

{% block content %}
<div style="max-width: 800px; margin: 0 auto;">
    <div style="background: white; padding: 32px; border-radius: 12px; box-shadow: 0 2px 8px rgba(0,0,0,0.08);">
        {{ form_start(form, {
            'attr': {
                'enctype': 'multipart/form-data',
                'method': 'POST'
            }
        }) }}

            <!-- Current Image Preview -->
            {% if product.image %}
                <div style="margin-bottom: 24px; text-align: center;">
                    <label style="
                        display: block;
                        font-size: 13px;
                        color: #888;
                        text-transform: uppercase;
                        font-weight: 600;
                        margin-bottom: 12px;
                    ">
                        Current Image
                    </label>
                    <img 
                        src="{{ asset('assets/images/wine_images/' ~ product.image) }}" 
                        alt="{{ product.name }}"
                        style="
                            max-width: 200px;
                            max-height: 200px;
                            border-radius: 8px;
                            border: 1px solid #e9ecef;
                        "
                    />
                </div>
            {% endif %}

            <!-- Product Name -->
            <div style="margin-bottom: 24px;">
                <label style="
                    display: block;
                    font-size: 13px;
                    color: #888;
                    text-transform: uppercase;
                    font-weight: 600;
                    margin-bottom: 8px;
                ">
                    Product Name *
                </label>
                {{ form_widget(form.name, {
                    attr: {
                        style: 'width: 100%; padding: 12px 16px; border: 2px solid #e9ecef; border-radius: 8px; font-size: 16px; font-family: inherit; transition: border-color 0.3s ease;',
                        class: 'form-input'
                    }
                }) }}
            </div>

            <!-- Description -->
            <div style="margin-bottom: 24px;">
                <label style="
                    display: block;
                    font-size: 13px;
                    color: #888;
                    text-transform: uppercase;
                    font-weight: 600;
                    margin-bottom: 8px;
                ">
                    Description
                </label>
                {{ form_widget(form.description, {
                    attr: {
                        style: 'width: 100%; padding: 12px 16px; border: 2px solid #e9ecef; border-radius: 8px; font-size: 16px; font-family: inherit; transition: border-color 0.3s ease; min-height: 100px;',
                        class: 'form-input'
                    }
                }) }}
            </div>

            <!-- Grid: Price and Category -->
            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 24px;">
                <!-- Price -->
                <div>
                    <label style="
                        display: block;
                        font-size: 13px;
                        color: #888;
                        text-transform: uppercase;
                        font-weight: 600;
                        margin-bottom: 8px;
                    ">
                        Price *
                    </label>
                    {{ form_widget(form.price, {
                        attr: {
                            style: 'width: 100%; padding: 12px 16px; border: 2px solid #e9ecef; border-radius: 8px; font-size: 16px; font-family: inherit; transition: border-color 0.3s ease;',
                            class: 'form-input'
                        }
                    }) }}
                </div>

                <!-- Category -->
                <div>
                    <label style="
                        display: block;
                        font-size: 13px;
                        color: #888;
                        text-transform: uppercase;
                        font-weight: 600;
                        margin-bottom: 8px;
                    ">
                        Category *
                    </label>
                    {{ form_widget(form.category, {
                        attr: {
                            style: 'width: 100%; padding: 12px 16px; border: 2px solid #e9ecef; border-radius: 8px; font-size: 16px; font-family: inherit; transition: border-color 0.3s ease;',
                            class: 'form-input'
                        }
                    }) }}
                </div>
            </div>

            <!-- Image Upload -->
            <div style="margin-bottom: 24px;">
                <label style="
                    display: block;
                    font-size: 13px;
                    color: #888;
                    text-transform: uppercase;
                    font-weight: 600;
                    margin-bottom: 8px;
                ">
                    Update Product Image
                </label>
                {{ form_widget(form.imageFile, {
                    attr: {
                        style: 'padding: 12px 16px; border: 2px dashed #e9ecef; border-radius: 8px; font-size: 14px;',
                        class: 'form-input',
                        accept: 'image/*'
                    }
                }) }}
                <p style="font-size: 12px; color: #999; margin-top: 6px;">
                    Leave empty to keep current image
                </p>
            </div>

            <!-- Form Errors -->
            {% if form.vars.errors %}
                <div style="
                    background: #f8d7da;
                    border: 1px solid #f5c6cb;
                    color: #721c24;
                    padding: 16px 20px;
                    border-radius: 8px;
                    margin-bottom: 20px;
                ">
                    <strong>Form Errors:</strong>
                    {{ form_errors(form) }}
                </div>
            {% endif %}

            <!-- Submit Buttons -->
            <div style="display: flex; gap: 12px; margin-top: 32px; border-top: 1px solid #eee; padding-top: 24px;">
                <button 
                    type="submit"
                    style="
                        flex: 1;
                        padding: 12px 20px;
                        background: linear-gradient(135deg, #7F1734, #9E2B47);
                        color: white;
                        border: none;
                        border-radius: 8px;
                        font-weight: 600;
                        cursor: pointer;
                        transition: all 0.3s ease;
                        font-size: 14px;
                    "
                    onmouseover="this.style.transform='translateY(-2px)'; this.style.boxShadow='0 8px 16px rgba(127, 23, 52, 0.3)'"
                    onmouseout="this.style.transform='translateY(0)'; this.style.boxShadow='none'"
                >
                    💾 Save Changes
                </button>
                <a 
                    href="{{ path('staff_products_show', {'id': product.id}) }}"
                    style="
                        flex: 1;
                        padding: 12px 20px;
                        text-align: center;
                        background: #f0f0f0;
                        color: #333;
                        text-decoration: none;
                        border-radius: 8px;
                        font-weight: 600;
                        transition: all 0.3s ease;
                        font-size: 14px;
                    "
                    onmouseover="this.style.background='#e0e0e0'"
                    onmouseout="this.style.background='#f0f0f0'"
                >
                    ← Cancel
                </a>
            </div>

            {{ form_end(form) }}
        </div>
    </div>

<script>
    document.querySelectorAll('.form-input').forEach(input => {
        input.addEventListener('focus', function() {
            this.style.borderColor = '#7F1734';
        });
        input.addEventListener('blur', function() {
            this.style.borderColor = '#e9ecef';
        });
    });
</script>
{% endblock %}
