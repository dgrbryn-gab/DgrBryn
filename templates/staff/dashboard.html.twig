{% extends 'staff/staff_base.html.twig' %}

{% block title %}Staff Dashboard - CORK{% endblock %}

{% block page_title %}Dashboard{% endblock %}

{% block content %}
<div class="mb-8">
    <!-- Statistics Cards -->
    <div class="dashboard-grid grid-cols-5 mb-8">
        
        <!-- Total Records Created -->
        <div class="stat-card burgundy">
            <div class="flex items-center justify-between">
                <div>
                    <span class="stat-label">Inventory Records</span>
                    <h3 class="stat-value">{{ myInventoryRecords }}</h3>
                </div>
                <span class="text-4xl">📦</span>
            </div>
        </div>

        <!-- Total Orders -->
        <div class="stat-card burgundy-light">
            <div class="flex items-center justify-between">
                <div>
                    <span class="stat-label">Total Orders</span>
                    <h3 class="stat-value">{{ myOrders }}</h3>
                </div>
                <span class="text-4xl">🛒</span>
            </div>
        </div>

        <!-- Pending Orders -->
        <div class="stat-card orange">
            <div class="flex items-center justify-between">
                <div>
                    <span class="stat-label">Pending Orders</span>
                    <h3 class="stat-value">{{ pendingOrders }}</h3>
                </div>
                <span class="text-4xl">⏳</span>
            </div>
        </div>

        <!-- Processing Orders -->
        <div class="stat-card green">
            <div class="flex items-center justify-between">
                <div>
                    <span class="stat-label">Processing Orders</span>
                    <h3 class="stat-value">{{ processingOrders }}</h3>
                </div>
                <span class="text-4xl">⚙️</span>
            </div>
        </div>
    </div>

    <!-- Quick Actions -->
    <div class="dashboard-card mb-8">
        <div class="dashboard-card-header">
            ⚡ Quick Actions
        </div>
        <div class="dashboard-card-body">
            <div class="grid grid-cols-5 gap-4">
                <a href="{{ path('staff_products_new') }}" class="action-link burgundy">
                    <span class="action-icon">🍷</span>
                    <span>Add Product</span>
                </a>

                <a href="{{ path('staff_inventory_new') }}" class="action-link burgundy">
                    <span class="action-icon">➕</span>
                    <span>Add Inventory</span>
                </a>

                <a href="{{ path('staff_orders_index') }}" class="action-link burgundy-light">
                    <span class="action-icon">📋</span>
                    <span>View Orders</span>
                </a>

                <a href="{{ path('staff_inventory_index') }}" class="action-link beige">
                    <span class="action-icon">📦</span>
                    <span>My Inventory</span>
                </a>

                <a href="{{ path('staff_products_index') }}" class="action-link beige">
                    <span class="action-icon">🍾</span>
                    <span>My Products</span>
                </a>
            </div>
        </div>
    </div>

    <!-- Welcome Section -->
    <div class="dashboard-card">
        <div class="dashboard-card-header">
            📋 Welcome to Your Staff Dashboard
        </div>
        <div class="dashboard-card-body">
            <div class="welcome-box">
                <p>
                    <strong>👋 Welcome!</strong> You have full access to manage inventory records and orders assigned to you. 
                    Use the navigation menu on the left to access different features. 
                    You can create new inventory records, view and manage your orders, and update your profile at any time.
                </p>
            </div>
        </div>
    </div>
</div>
{% endblock %}
