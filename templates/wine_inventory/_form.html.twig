<div class="max-w-6xl mx-auto px-4 py-8">
    {{ form_start(form, {'attr': {'class': 'space-y-4'}}) }}
        {{ form_widget(form, {'attr': {'class': 'w-full p-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-amber-500'}}) }}
        <button class="text-white bg-gray-900 hover:bg-amber-500 hover:text-white font-medium py-2 px-4 rounded transition-colors duration-300">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}
</div>