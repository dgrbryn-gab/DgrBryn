<form method="post" action="{{ path('app_wine_inventory_delete', {'id': wine_inventory.id}) }}" onsubmit="return confirm('Are you sure you want to delete this inventory record?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wine_inventory.id) }}">
    <button type="submit" 
            class="px-4 py-2 font-semibold rounded-xl shadow-md transition-all duration-300"
            style="background-color: #b91c1c; color: #f4e7ce; border: 1px solid #f9d3b4;"
            onmouseover="this.style.backgroundColor='#991b1b';"
            onmouseout="this.style.backgroundColor='#b91c1c';">
        🗑 Delete
    </button>
</form>