<form method="post" action="{{ path('app_wine_inventory_delete', {'id': wine_inventory.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wine_inventory.id) }}">
    <button class="text-white bg-gray-900 hover:bg-amber-500 hover:text-white font-medium py-1 px-3 rounded transition-colors duration-300">Delete</button>
</form>