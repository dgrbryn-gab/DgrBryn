{% extends 'admin/admin_base.html.twig' %}

{% block title %}Orders - CORK Admin{% endblock %}

{% block admin_content %}

<div class="min-h-screen bg-gradient-to-b from-[#FFF8F0] to-[#F8EAD8] py-14 px-6">
    <div class="max-w-7xl mx-auto">
        <!-- Flash Messages -->
        {% for message in app.flashes('success') %}
            <div class="mb-6 p-4 rounded-lg bg-green-50 border-l-4 border-green-500 text-green-700">
                <p class="font-semibold">✓ {{ message }}</p>
            </div>
        {% endfor %}
        {% for message in app.flashes('error') %}
            <div class="mb-6 p-4 rounded-lg bg-red-50 border-l-4 border-red-500 text-red-700">
                <p class="font-semibold">✕ {{ message }}</p>
            </div>
        {% endfor %}

        <!-- Header Section -->
        <div class="flex flex-col sm:flex-row justify-between items-center mb-10">
            <div class="text-center sm:text-left mb-4 sm:mb-0">
                <h1 class="text-4xl font-extrabold text-[#5E0B15] tracking-tight mb-2">Orders Management</h1>
                <p class="text-gray-600 text-sm italic">View and manage all customer orders.</p>
            </div>
            <a href="{{ path('app_order_new') }}"
                class="px-6 py-2 rounded-xl font-semibold shadow-md transition-all duration-300 border text-sm sm:text-base bg-[#EFCFB4] text-[#7F1734] border-[#EFCFB4] hover:bg-[#7F1734] hover:text-white">
                + Create Order
            </a>
        </div>

        <!-- Status Filter -->
        <div class="mb-8 flex flex-wrap gap-3">
            <a href="{{ path('app_order_index') }}"
                class="px-4 py-2 rounded-lg text-sm font-semibold transition-all duration-300 {% if not currentStatus %}bg-[#7F1734] text-white{% else %}bg-white text-[#7F1734] border border-[#EFCFB4]{% endif %}">
                All Orders
            </a>
            {% for status in ['pending', 'processing', 'shipped', 'delivered', 'cancelled'] %}
                <a href="{{ path('app_order_index', {'status': status}) }}"
                    class="px-4 py-2 rounded-lg text-sm font-semibold transition-all duration-300 {% if currentStatus == status %}bg-[#7F1734] text-white{% else %}bg-white text-[#7F1734] border border-[#EFCFB4]{% endif %}">
                    {{ status|capitalize }}
                </a>
            {% endfor %}
        </div>

        <!-- Orders Table -->
        {% if orders|length > 0 %}
            <div class="bg-white/90 backdrop-blur-sm border border-[#EFCFB4]/50 rounded-2xl shadow-md overflow-hidden">
                <div class="overflow-x-auto">
                    <table class="w-full">
                        <thead>
                            <tr class="bg-gray-100 border-b-2 border-[#5E0B15]">
                                <th class="px-6 py-4 text-left text-sm font-semibold text-[#5E0B15]">Order #</th>
                                <th class="px-6 py-4 text-left text-sm font-semibold text-[#5E0B15]">Customer</th>
                                <th class="px-6 py-4 text-left text-sm font-semibold text-[#5E0B15]">Date</th>
                                <th class="px-6 py-4 text-right text-sm font-semibold text-[#5E0B15]">Total</th>
                                <th class="px-6 py-4 text-center text-sm font-semibold text-[#5E0B15]">Status</th>
                                <th class="px-6 py-4 text-center text-sm font-semibold text-[#5E0B15]">Payment</th>
                                <th class="px-6 py-4 text-center text-sm font-semibold text-[#5E0B15]">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for order in orders %}
                                <tr class="border-t border-[#EFCFB4]/30 hover:bg-[#F8EAD8]/50 transition-colors">
                                    <td class="px-6 py-4 text-sm font-semibold text-[#5E0B15]">
                                        {{ order.orderNumber }}
                                    </td>
                                    <td class="px-6 py-4 text-sm text-gray-700">
                                        {{ order.customerName ?? 'N/A' }}
                                    </td>
                                    <td class="px-6 py-4 text-sm text-gray-600">
                                        {{ order.createdAt ? order.createdAt|date('M j, Y H:i') : 'N/A' }}
                                    </td>
                                    <td class="px-6 py-4 text-sm font-semibold text-right text-[#5E0B15]">
                                        ₱{{ order.totalAmount }}
                                    </td>
                                    <td class="px-6 py-4 text-center">
                                        <span class="px-3 py-1 rounded-full text-xs font-semibold
                                            {% if order.status == 'pending' %}bg-yellow-100 text-yellow-700
                                            {% elseif order.status == 'processing' %}bg-blue-100 text-blue-700
                                            {% elseif order.status == 'shipped' %}bg-purple-100 text-purple-700
                                            {% elseif order.status == 'delivered' %}bg-green-100 text-green-700
                                            {% elseif order.status == 'cancelled' %}bg-red-100 text-red-700
                                            {% endif %}">
                                            {{ order.status|capitalize }}
                                        </span>
                                    </td>
                                    <td class="px-6 py-4 text-center">
                                        <span class="px-3 py-1 rounded-full text-xs font-semibold
                                            {% if order.paymentStatus == 'completed' %}bg-green-100 text-green-700
                                            {% elseif order.paymentStatus == 'pending' %}bg-yellow-100 text-yellow-700
                                            {% elseif order.paymentStatus == 'failed' %}bg-red-100 text-red-700
                                            {% else %}bg-gray-100 text-gray-700
                                            {% endif %}">
                                            {{ order.paymentStatus ?? 'Pending' }}
                                        </span>
                                    </td>
                                    <td class="px-6 py-4 text-center">
                                        <div class="flex gap-2 justify-center">
                                            <a href="{{ path('app_order_show', {'id': order.id}) }}"
                                                class="text-sm font-semibold px-3 py-1 rounded-lg border transition-all duration-300 bg-[#EFCFB4] text-[#7F1734] border-[#EFCFB4] hover:bg-[#E3B78E] btn-hover inline-block">
                                                👁️ View
                                            </a>
                                            <form method="post" action="{{ path('app_order_delete', {'id': order.id}) }}" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this order? This action cannot be undone.');">
                                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order.id) }}">
                                                <button type="submit" class="text-sm font-semibold px-3 py-1 rounded-lg border transition-all duration-300 bg-red-100 text-red-700 border-red-300 hover:bg-red-500 hover:text-white btn-hover">
                                                    🗑 Delete
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        {% else %}
            <div class="text-center py-24 bg-white/90 backdrop-blur-sm border border-[#EFCFB4]/50 rounded-2xl">
                <p class="text-gray-600 text-lg">📦 No orders found.</p>
                <p class="text-gray-500 text-sm mt-2">Start creating orders to manage them here.</p>
            </div>
        {% endif %}
    </div>
</div>

{% endblock %}
