{% extends 'admin/admin_base.html.twig' %}

{% block title %}Category Details | CORK{% endblock %}

{% block admin_content %}
<div class="min-h-screen bg-gradient-to-b from-[#FFF8F0] to-[#F8EAD8] py-14 px-6">
    <div class="max-w-3xl mx-auto bg-white/90 backdrop-blur-sm border border-[#EFCFB4]/50 rounded-2xl shadow-md p-10">
        <h1 class="text-3xl font-extrabold text-[#5E0B15] mb-8 text-center">🍇 Category Details</h1>

        <!-- Info Card -->
        <div class="space-y-6 text-gray-700">
            <div class="space-y-3">
                <div class="flex justify-between items-center bg-burgundy text-cream border border-gold rounded-lg px-5 py-3 shadow-md hover:bg-burgundy-light transition-all duration-300">
                    <span class="font-semibold text-cream-light">ID:</span>
                    <span>{{ category.id }}</span>
                </div>

                <div class="flex justify-between items-center bg-burgundy text-cream border border-gold rounded-lg px-5 py-3 shadow-md hover:bg-burgundy-light transition-all duration-300">
                    <span class="font-semibold text-cream-light">Name:</span>
                    <span>{{ category.name }}</span>
                </div>
            </div>




            <div class="flex justify-between border-b border-[#EFCFB4]/50 pb-3">
                <span class="font-semibold text-[#5E0B15]">Description:</span>
                <span>{{ category.description ?: 'No description available.' }}</span>
            </div>

            <div class="flex justify-between border-b border-[#EFCFB4]/50 pb-3">
                <span class="font-semibold text-[#5E0B15]">Created At:</span>
                <span>{{ category.createdAt ? category.createdAt|date('M j, Y | H:i A') : 'N/A' }}</span>
            </div>
        </div>

        <!-- Buttons -->
        <div class="flex justify-end gap-4 mt-10">
            <a href="{{ path('app_category_index') }}"
               class="px-6 py-2 font-semibold rounded-xl border transition-all duration-300"
               style="background-color: #EFCFB4; color: #7F1734; border-color: #EFCFB4;"
               onmouseover="this.style.backgroundColor='#E3B78E'"
               onmouseout="this.style.backgroundColor='#EFCFB4'">
                ⬅ Back to List
            </a>

            <a href="{{ path('app_category_edit', {'id': category.id}) }}"
               class="px-6 py-2 font-semibold rounded-xl border transition-all duration-300"
               style="background-color: #7F1734; color: #F8EAD8; border-color: #EFCFB4;"
               onmouseover="this.style.backgroundColor='#9E2B47'"
               onmouseout="this.style.backgroundColor='#7F1734'">
                ✏️ Edit
            </a>

            {{ include('category/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
