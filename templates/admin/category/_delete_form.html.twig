<form method="post" action="{{ path('app_category_delete', {'id': category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this category?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
    <button type="submit"
            class="text-sm font-semibold px-4 py-1.5 rounded-lg border transition-all duration-300 bg-[#B6465F] text-[#F8EAD8] border-[#EFCFB4] hover:bg-[#9E2B47]">
        🗑 Delete
    </button>
</form>
