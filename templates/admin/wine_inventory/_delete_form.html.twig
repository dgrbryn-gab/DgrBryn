<form method="post" action="{{ path('app_wine_inventory_delete', {'id': wine_inventory.id}) }}" onsubmit="return confirm('Are you sure you want to delete this inventory record?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wine_inventory.id) }}">
    <button type="submit"
            class="px-4 py-2 font-semibold rounded-xl shadow-md transition-all duration-300 border"
            style="background-color: #7F1734; color: #f4e7ce; border-color: #f9d3b4;"
            onmouseover="this.style.backgroundColor='#6b132a';"
            onmouseout="this.style.backgroundColor='#7F1734';">
        🗑 Delete
    </button>
</form>
