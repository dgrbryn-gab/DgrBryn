# Enable CSRF protection with session-based token storage
framework:
    form:
        csrf_protection:
            enabled: true

    csrf_protection:
        enabled: true
        # Use session storage instead of stateless tokens

